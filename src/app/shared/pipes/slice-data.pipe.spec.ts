import { SliceDataPipe } from './slice-data.pipe';

describe('SliceDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceDataPipe();
    expect(pipe).toBeTruthy();
  });
});
